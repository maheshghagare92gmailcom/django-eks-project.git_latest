apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: efs-sc
provisioner: efs.csi.aws.com
parameters:
  provisioningMode: efs-ap
  fileSystemId: fs-0e4cf82927cdaef7f # Change this to your EFS ID
  directoryPerms: "755"
  basePath: "/dynamic_provisioning"
