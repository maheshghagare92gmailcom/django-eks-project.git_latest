apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: efs-sc
provisioner: efs.csi.aws.com
parameters:
  provisioningMode: efs-ap
  fileSystemId: fs-07ab78a648c31c7f6 # Change this to your EFS ID
  directoryPerms: "755"
  basePath: "/dynamic_provisioning"
